//A forEach loop is a method on arrays that executes a function for each element in the array.

const arr = [1,2,3,4,5];

arr.forEach(i => console.log(i));

//result should be for each iteration of array;